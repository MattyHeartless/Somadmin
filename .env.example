# Frontend
NEXT_PUBLIC_API_URL=http://localhost:5000

# API bootstrap. Set these in the shell that starts the API; do not commit .env.local.
BootstrapAdmin__Email=admin@soma.local
BootstrapAdmin__Password=ReplaceWithAStrongPassword!

# Docker SQL Server-compatible development database
MSSQL_SA_PASSWORD=ChangeThisBeforeDeploying!
